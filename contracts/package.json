{
  "name": "noslip-web3-contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat ignition deploy ignition/modules/NoSlip.ts --network localhost",
    "deploy:base-sepolia": "hardhat ignition deploy ignition/modules/NoSlip.ts --network baseSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^5.0.7",
    "@openzeppelin/contracts": "^5.6.1",
    "hardhat": "^3.9.0"
  }
}
